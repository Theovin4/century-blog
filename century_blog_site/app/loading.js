export default function Loading() {
  return (
    <main className="page-shell">
      <section className="section-card loading-panel" aria-live="polite">
        <span className="eyebrow">Loading</span>
        <h1>Preparing the page</h1>
        <p className="hero-text">Century Blog is loading the latest content for you.</p>
      </section>
    </main>
  );
}
